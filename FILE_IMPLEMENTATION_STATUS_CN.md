# 代码文件实现现状（按头文件与源文件）

## include/AI.h
- 已实现：声明了`AI::getBestMove`和`AI::getWonderChoice`用于决策。 
- 未实现：未提供更高阶评估接口或与Game回合流程的集成细节。

## src/AI.cpp
- 已实现：`getBestMove`根据可访问卡牌、可负担性和基础权重评估简化选择；`getWonderChoice`随机返回奇迹索引。 
- 未实现：未区分建造/弃牌/造奇迹三种行动，未使用军事/科学胜负条件或进步标记；未和Game的行动执行绑定。

## include/AIPlayer.h
- 已实现：定义AIPlayer继承Player的构造与析构接口。 
- 未实现：未声明决策/评估方法（只在cpp中自定义，接口与基类不一致）。

## src/AIPlayer.cpp
- 已实现：构造函数设置随机种子；提供`makeDecision`和`evaluateCard`示例逻辑输出评估信息。 
- 未实现：函数签名与Player抽象类不匹配（基类纯虚`makeDecision`返回`int`），且未真正执行建造/弃牌操作。

## include/Board.h
- 已实现：金字塔槽位、军事位置、奇迹池与进步标记容器；提供时代布局`setupAge`/`setupAge1/2/3`、卡牌可达判断与翻牌、军事移动、奇迹与进步标记的获取接口。 
- 未实现：未存储弃牌堆/军事惩罚标记等公共状态；进步标记事件`onSciencePair`缺少对Game/玩家的回调接口说明。

## src/Board.cpp
- 已实现：三代金字塔布局、可达判断、拿牌与翻牌、军事位置累加、奇迹列表存取、进步标记初始化/洗牌/抽取/获取、科学符号计数及6符号胜利占位检查。 
- 未实现：军事区段罚款与即时胜利处理留空（仅累加位置）；`onSciencePair`只计数未触发玩家选择或游戏结束；未与Game/玩家状态同步扣币、弃牌堆等。

## include/Card.h
- 已实现：卡牌名称、类型、成本、效果与链符号的访问器。 
- 未实现：无多语言/描述字段；未标识是否为正反面、所属时代等元数据。

## src/Card.cpp
- 已实现：构造函数与各字段的getter。 
- 未实现：无额外逻辑或效应解析，未覆盖复制/比较等辅助操作。

## include/CardFactory.h
- 已实现：声明了创建不同类型卡牌（原料、制造品、军事、科学、商业、民用、公会）的工厂接口与单例获取。 
- 未实现：缺少具体卡牌列表说明与Guild/时代配置；未暴露进步标记或奇迹创建辅助。

## src/CardFactory.cpp
- 已实现：给出多种`createXxxCard`实现，封装成本与效果设置。 
- 未实现：时代牌组函数不完整（部分返回空或未覆盖所有卡）；商业折扣等效果字段未填充。

## include/ConsoleView.h
- 已实现：声明用于输出信息、错误、提示和展示游戏状态的静态方法。 
- 未实现：无输入处理、无格式化细节，未与UI层分离。

## src/ConsoleView.cpp
- 已实现：简单`printInfo/printError/printPrompt/printBoardState`控制台输出。 
- 未实现：未渲染金字塔或玩家面板的完整状态，未支持交互输入。

## include/Deck.h
- 已实现：静态方法获取时代牌组与奇迹列表，并支持可选洗牌模板。 
- 未实现：`createWonders`未在头文件或cpp完整声明配套；返回类型与`CardFactory`使用的共享指针/裸指针混杂。

## src/Deck.cpp
- 已实现：辅助`createCard`构建卡牌，部分Age I卡组样例，添加随机洗牌工具。 
- 未实现：Age II/III、公会与弃三张规则缺失；卡牌常量错误（符号枚举与Effect变量命名混乱），函数签名与头文件不匹配（头文件返回`shared_ptr`而cpp返回裸指针向量）。

## include/Game.h
- 已实现：保存玩家指针、棋盘、当前时代与回合标记；声明初始化、开始游戏、进行回合、胜利检测、军事处理、奇迹挑选等。 
- 未实现：未声明弃牌堆、银行、进步标记或额外回合等关键状态；接口与Player构造/AI选择存在不一致。

## src/Game.cpp
- 已实现：初始化玩家与棋盘、奇迹选择两个轮次并随机先手，基础回合循环框架（`playAge`/`playTurn`）以及军事胜利检查占位。 
- 未实现：多数逻辑留空或退出（如奇迹选择后`exit`）；未实现三种行动流程、支付/建造/弃牌、年龄推进、军事/科学即时胜利、计分与额外回合等。

## include/HumanPlayer.h
- 已实现：声明HumanPlayer继承Player的构造与析构。 
- 未实现：未扩展输入/交互接口。

## include/Player.h
- 已实现：玩家基础属性（金币、军力、分数、资源、科学符号、已建卡/奇迹）；成本计算、支付、建造卡与奇迹、资源/符号统计等。 
- 未实现：缺少弃牌堆访问、进步标记与交易折扣状态；`makeDecision`为纯虚返回`int`但AI实现不匹配；未处理“额外回合”等标志。

## src/Player.cpp
- 已实现：初始化7金币，资源/符号/卡片/奇迹的添加；按对手棕灰产量计算交易费用并支持链条免费；建造时应用VP/盾牌/金币/资源/科学符号效果。 
- 未实现：未过滤奇迹与黄牌产出的资源对交易成本的影响；未处理商业折扣字段或弃牌收益；未触发进步标记/军事移动/科学胜利等游戏事件。

## include/Resources.h
- 已实现：资源、卡牌类型、科学符号枚举；成本与效果结构，效果包含产出、VP、盾牌、科学符号、金币和`playAgain/resourceRebate`标志。 
- 未实现：缺少商业折扣或交易成本字段，科学符号枚举与实际卡牌使用不完全一致。

## include/Wonder.h
- 已实现：奇迹名称、成本、效果、建成标记及`activateEffect`声明。 
- 未实现：奇迹效果细节未枚举，未包含“已弃置第八张”或跨局可用性等状态。

## src/Wonder.cpp
- 已实现：getter、`build`标记、简单`activateEffect`调用玩家获得资源/军力/VP/金币/科学符号。 
- 未实现：未处理奇迹特有效果（额外回合、弃掉对手卡、破坏资源、军事推进等）；未与Game状态同步或限制全局7座奇迹规则。

## src/CardFactory.cpp
- 已实现：提供各类`createXxxCard`函数，封装成本与效果构造。 
- 未实现：未维护完整牌池或链符号/时代分配，部分效果字段空缺；未支持公会/扩展牌。

## src/Card.cpp
- 已实现：卡牌字段存储与访问。 
- 未实现：无额外逻辑。

## src/ConsoleView.cpp
- 已实现：控制台输出基础消息。 
- 未实现：缺少状态展示和输入。

## src/main.cpp
- 已实现：命令行模式下创建游戏、读取玩家信息并启动。 
- 未实现：未处理AI/Human类的差异化实例化、无错误控制和回合交互细节。
