# 7 Wonders Duel 规则修复分步计划

以下步骤基于 `RULE_GAP_REPORT.md` 与 `FILE_IMPLEMENTATION_STATUS_CN.md` 中的缺口，总结为可按顺序执行的修复路径，优先级从影响胜负的核心机制到次要流程。每一步的目标与建议修改点均指向具体文件，便于分阶段提交。

## 步骤 1：完善核心状态模型
- **目标**：为后续规则提供所需的持久状态。
- **修改点**：
  - `include/Game.h` / `src/Game.cpp`：增加弃牌堆、银行金币（或抽象银行）、已移除/已获进步标记池、已建奇观计数与“第八座移除”标志、额外回合标志。
  - `include/Player.h` / `src/Player.cpp`：增加进步标记集合、交易折扣（黄牌/进步/奇观来源）、弃牌收益系数缓存（黄牌数量）、科学符号计数器（含进步 Law）。
  - `include/Board.h` / `src/Board.cpp`：增加军事区段 token 配置与已移除状态，公共进步标记可选池，冲突棋子初始位置常量。

## 步骤 2：准备阶段与牌库构建
- **目标**：按规则完成三时代牌堆与奇观/进步标记的初始化。
- **修改点**：
  - `src/Deck.cpp`：补全 Age I/II/III 牌列表与公会牌，加入“每时代随机移除 3 张”逻辑，Age III 插入随机 3 张公会牌，保持洗牌与卡面朝向信息。
  - `src/Game.cpp`：`init` 中调用 `Board::setupProgressTokens` 抽取 5 枚可选进步标记，其余标记移除；初始化冲突棋子为 0；玩家初始 7 金币；奇观选择流程使用 12 张奇观，按 4/4/4/4 拿牌并限制全局最多 7 座。

## 步骤 3：Age 摆阵与可访问牌逻辑
- **目标**：确保每时代的金字塔布局、翻牌和可选牌合法。
- **修改点**：
  - `src/Board.cpp`：在 `setupAge` 中支持传入“被移除的 3 张”后构建布局，保持 face-up/face-down 布局；`revealAvailableCards` 在移除覆盖牌后自动翻面。
  - `src/Game.cpp`：在 Age 结束时重建下一 Age 的牌阵并揭示可访问牌。

## 步骤 4：回合动作管线
- **目标**：实现三种合法行动及顺序控制。
- **修改点**：
  - `src/Game.cpp`：`playTurn` 支持“建造建筑 / 弃牌 / 建造奇观”三选一；弃牌收入=2+黄牌数，牌入弃牌堆；建造奇观消耗时代牌（牌本身无效果）。
  - `src/Player.cpp`：提供执行建造/弃牌/建造奇观的接口，调用后返回军力/科学/金币等变化以便 Game 处理；在奇观/进步的 playAgain 场景保持当前玩家连续行动。

## 步骤 5：成本计算与交易折扣
- **目标**：按官方规则计算费用并验证可支付性。
- **修改点**：
  - `src/Player.cpp`：`calculateCost` 区分本方产出、对手棕灰产出、黄牌/奇观产出；交易成本=2+对手对应资源数量；黄牌/奇观折扣使资源单价=1；支持链条免费、金币成本与资源成本混合；在支付前检查资源可覆盖。
  - `include/Resources.h`：补充商业折扣字段以描述资源单价=1 或金币减免。

## 步骤 6：奇观系统
- **目标**：完整建造与效果解析。
- **修改点**：
  - `include/Wonder.h` / `src/Wonder.cpp`：为 12 张奇观填充成本和效果（盾牌/VP/金币/资源/科学符号/弃对手卡/额外回合等）；`build` 应标记已建并返回效果数据。
  - `src/Game.cpp`：执行奇观效果（如军事移动、金币/VP 奖励、销毁对手棕灰资源或一张未建卡、触发额外回合）；建造第 7 座后移除剩余奇观。

## 步骤 7：进步标记与科学胜利
- **目标**：科学符号配对与进步标记获取、即时胜利。
- **修改点**：
  - `src/Board.cpp`：`onSciencePair` 检测到两张相同符号时，让当前玩家从公共 5 枚中选择一枚；Law 作为符号计入科学胜利；科学符号达到 6 种立即胜利。
  - `src/Game.cpp`：建造后调用 science 检查并在即时胜利时终止流程；应用进步标记的被动效果（折扣、额外符号、VP 等）到玩家状态。

## 步骤 8：军事轨与惩罚
- **目标**：移动冲突棋子、触发区段 token 与军事胜利。
- **修改点**：
  - `src/Board.cpp`：在 `moveMilitary` 中按盾牌数量移动、夹紧到 -9~9，跨越区段时触发 token（扣对手金币、VP、额外盾等），触发后移除该 token。
  - `src/Game.cpp`：在每次军事移动后检查是否到达对手首都并立即军事胜利；金币不足时扣到 0。

## 步骤 9：Age 结束与先手
- **目标**：清空牌阵后推进时代并确定下一时代先手。
- **修改点**：
  - `src/Game.cpp`：当金字塔为空时结束 Age；若冲突棋子偏向一侧则该侧决定先手，否则上一回合行动玩家决定；重新调用 `Board::setupAgeX` 构建下一 Age。

## 步骤 10：终局计分与平局
- **目标**：无即时胜利时的最终结算。
- **修改点**：
  - `src/Game.cpp` / `src/Player.cpp`：计算军事轨分、蓝/绿/黄/紫牌 VP、奇观 VP、进步 VP、每 3 金币 1 分；平局时比较蓝牌 VP，再次平局则并列。

## 步骤 11：AI 接口与人机流程
- **目标**：让 AI 回合可执行完整规则，Human 流程接口一致。
- **修改点**：
  - `include/AIPlayer.h` / `src/AIPlayer.cpp`：`makeDecision` 返回包含行动类型与目标牌/奇观；使用 `Player` 基类提供的可负担性检查。
  - `src/Game.cpp`：根据决策调用对应行动；人类输入与 AI 选择共用同一管线。

## 步骤 12：自动化测试基线
- **目标**：为后续开发提供可重复验证的用例。
- **修改点**：
  - 为关键模块添加单元/集成测试（可用 gtest 或自定义断言）：交易价格、链条免费、弃牌收益、奇观第七座移除、军事区段触发、科学配对取标记、playAgain 连续回合、Age 结束先手与终局计分等。

> 建议按上述顺序逐步提交，确保每一步都可编译并具备最小回归测试，用例参考 `RULE_GAP_REPORT.md` 中的“Required Next Steps”与测试建议。
